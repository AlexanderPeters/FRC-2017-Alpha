package main.commands.drivetrain;

import edu.wpi.first.wpilibj.command.CommandGroup;
import main.Constants;
import main.Robot;

public class AlignToGoal extends CommandGroup implements Constants {
	//if(Robot.comms.getTargetFound())
		// addSequential (new (TurnToAngle(Robot.comms.getBearing(), Robot.kToleranceDegreesDefault));
	

}
